"""
State management (menu/fighting/pause/game over), 
health system (100 HP per fighter), 
timer (2-min rounds), 
win conditions

reads inputs → processes motors → reads sensors → calculates damage → checks win condition
"""

if __name__ == "__main__":
    print("This is... the maaaain event. of. the evening!")
